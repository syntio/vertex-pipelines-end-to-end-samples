  # Core pipeline dependency
  kfp==1.8.21

  # Template engine (needed by main pipeline code)
  jinja2>=3.0.1,<4.0.0

  # Local components (editable installs)
  -e ../components/bigquery-components/
  -e ../components/vertex-components/

  # GCP libraries (already installed but with specific versions)
  google-cloud-aiplatform==1.24.1
  google-cloud-bigquery==2.30.0
  google-cloud-pipeline-components==1.0.42

  # Testing
  pytest>=7.3.1,<8.0.0

  # Already have compatible versions, but listing for completeness
  google-cloud-storage==2.10.0